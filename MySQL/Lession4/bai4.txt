*** LỆNH ON DELETE CASCADE : Thêm ngay chỗ  forieng key 
=> Là lênh tắt forieng key ( Không cần phải xóa câu lệnh con nữa )

VD :FOREIGN KEY   (ExamID )  REFERENCES Exam (ExamID ) ON DELETE CASCADE,

*** INNER JOIN 
    LEFT JOIN
    RIGHT JOIN
    CROSS JOIN
** lỆNH  Right join , Left Join : select from từ bảng nào thì bảng đó sẽ 
Phân tích là nằm bên trái => mục đích để chọn left hay right .

*** UNION chọn thông tin riêng biệt và liên quan từ hai bảng 
=> sẽ loại bỏ các hàng trùng lặp

** UNION ALL chọn tất cả các giá trị từ cả hai bảng, hiển thị tất cả các hàng. ... 
 "UNION ALL" không xóa các hàng trùng lặp.